# 本地检查记录

日期：2026-09-19。没有启动正式训练，也没有部署或修改服务器项目。

- 7项CPU自动检查通过：最新协议约束、两模型前向/反向、RGB范围、无mask参数、top-1实际专家执行、固定配对与翻转、七扰动与动态序列复现、与上游BasicSR数值对照、完整回合恢复及400回合报告汇总（若干检查合并在同一测试方法中）。
- 控制恢复与报告测试使用明确的合成fixture，不是实际DMC回报；fixture只位于临时目录，不进入正式输出。
- 全部项目Python文件语法编译通过；experiment.py CLI帮助可运行。
- 本地十个checkpoint文件及配置预检通过，均为64×64、repeat=2；校验SHA256并保存LOCAL_POLICY_PREFLIGHT.json。
- 本轮未运行十任务JAX实时结构加载，未验证服务器CUDA训练batch128显存，未采集正式数据。代码中的严格加载及运行时检查会在服务器实际执行。
- ACO-RGB可训练参数：2,649,181；SMFA-RGB：182,703。无掩码分支。
- ACO参考代码为工作区内已有修改版本，不能宣称原作者未修改官方实现；固定来源见source_manifest.json。

运行本地检查：`python tests.py`。服务器使用Python 3.12；本地检查在已有Python 3.10/PyTorch 2.5.1环境完成，不等同于验证服务器所有依赖安装。
